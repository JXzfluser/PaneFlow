# Gate 0 裁决（初版 · 2026-09-19，仅从实机证据写）

> 判据：herdr pane 编排这条路线是否值得继续投入（Q1）。
> 红线：本文只允许记录**实际发生过的运行**；未做的项显式标开放，不得先造结论。
> 2026-09-18 口头裁决（`ce42f96`）解除了 v8 G/M/H/I 的实施门控，本文件是事后按实据补的 verdict 本体。

## 一、实机证据账（2026-09-18 → 09-19）

### 成立的
- **端到端绿跑 1 条**：run `cabc2242`（默认空间，pi agent，全链路走用户网关）——引擎开 pane、
  注入提示、agent 真实写产物（`hello.txt` 内容核对）、节点 done、run completed。
  证明编排回路（pane 生命周期 + prompt 投递 + 产物回收 + 状态机）在真 herdr + 真 agent + 真网络上成立。
- **pi 顶掉坏 agent**：claude CLI 未登录（开箱即挂）场景下，AE 解析链让 pi 真起并接令（🖥 见 `fb9c3eb` 后冒烟）。
- **故障可见性**：以下事故全部被引擎如实暴露而非静默——这正是 F 批要的形状：
  - `agent_prompt_stalled`（45s 无状态变化）准确命中 pi 模型侧失败的每一次；
  - 网关免费池 500（`felo-web exhausted_connection:noauth`）→ 节点失败 + 时间线可查；
  - `136a82af` 失踪 run（重启后记录异常）→ 暴露后由 F2/G3 的重启可活路径覆盖测试。
- **排队/归档/货架实机可用**：queued 撤回、归档页签、产物下载实机点过（截图暴露的「排队卡没有
  下一步动作」已立项 v9-N3）。

### 不成立的 / 代价
- **两轮全红**：绿跑之前连挂 4 条 run（254cafaa、66f6b2da、70525024、9af0182b、861eb19b），
  根因全部在**环境**而非编排器：pi 的 provider 基址写死（不读 OPENAI_BASE_URL）、
  herdr 守护进程继承的死 HTTP_PROXY(127.0.0.1:20128) 劫持 pane 网络、网关免费池上游 500。
  → 结论：编排回路本身 0 次被判负；但「环境地雷排雷」占了本迭代实机时间的多数。
- **ci 从未绿过**：pnpm 双声明 8 秒即挂（`b8ca3d7` 已修）。
  → **2026-09-19 已验证收口**：主仓推送 `1b4ad8d..ee8820a`（59 提交）后 Actions 首跑
  run `35442097125` 全绿（install/typecheck/全量测试/web build 四步全 success）。

## 二、初步判定（可被后续复跑推翻）

**Q1 = 是（有条件）**：herdr pane 编排路线值得继续——判据是那条真实绿跑与故障可见性账；
条件是 v9 必须把「环境接入的地雷」当一等公民（网关对话级探测、班底/入口贴合），
而不是再加新能力。

## 三、开放项（未做，不得视为已证）

1. **3× 夜间无人值守复跑**（v8 出口条件 2 的「隔日第二条同类单自动注入画像」仍无实据）→ 完成后更新本判定。
2. H1 真实仓库首推（用户动作）。
3. ~~CI 推送后首跑绿~~ ✅ 2026-09-19 达成：run `35442097125` 四步全绿（见上账）。
4. 失败率没有分母：绿 1 / 红 5 是本机调试期样本，不构成稳定率结论——夜间复跑就是为了补这个分母。
